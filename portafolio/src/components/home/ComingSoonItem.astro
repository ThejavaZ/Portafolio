---


import { Image } from "astro:assets"
const { name, firstColor, secondColor }  = Astro.props
---

<Image  
transition:name=`${name}`
src=`/technologies/${name}.png` 
alt=`${name}`
width={250} 
height={250}
quality={95}
loading="lazy" 
format="webp" 
class=`w-24 h-24 object-contain  aspect-square block transition hover:cursor-pointer hover:shadow-2xl hover:shadow-${firstColor}-500 hover:border hover:border-${secondColor}-500 rounded py-2 px-1` />
